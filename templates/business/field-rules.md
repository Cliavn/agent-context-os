# 字段规则：<模块名>

状态：current
最后校验日期：<YYYY-MM-DD>

## 字段总表

| 展示名 | 内部字段 | 来源 | 类型 | 必填 | 默认值 | 校验规则 | 备注 |
|---|---|---|---|---|---|---|---|
| `<展示名>` | `<field_name>` | `<来源>` | `<类型>` | `<是/否>` | `<默认值>` | `<规则>` | `<备注>` |

## 字段映射

| 输入字段 | 标准字段 | 输出字段 | 转换规则 |
|---|---|---|---|
| `<输入>` | `<标准>` | `<输出>` | `<规则>` |

## 空值处理

| 字段 | 空值含义 | 处理方式 |
|---|---|---|
| `<字段>` | `<含义>` | `<处理方式>` |

## 易错字段

- `<字段>`：`<易错原因和防线>`

