# 业务模块总览：<模块名>

状态：current
最后校验日期：<YYYY-MM-DD>
关联代码路径：
- `<代码路径>`
关联测试：
- `<测试路径>`

## 模块目标

<说明这个业务模块解决什么问题。>

## 目标用户

| 用户 | 主要动作 | 成功标准 |
|---|---|---|
| `<用户>` | `<动作>` | `<标准>` |

## 核心业务对象

| 对象 | 说明 | 关键字段 |
|---|---|---|
| `<业务对象>` | `<说明>` | `<字段>` |

## 主流程摘要

```text
<步骤 1> -> <步骤 2> -> <步骤 3>
```

## 非目标

- `<本模块不处理的事项>`

## 关联文档

- `business-flow.md`
- `field-rules.md`
- `state-machine.md`
- `edge-cases.md`
- `acceptance.md`

