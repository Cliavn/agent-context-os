# 接口契约：<模块名>

状态：current
最后校验日期：<YYYY-MM-DD>

## 接口列表

| 接口 | 方法 | 说明 | 权限 |
|---|---|---|---|
| `<path>` | `<GET/POST/...>` | `<说明>` | `<权限>` |

## 请求结构

```json
{
  "field": "<value>"
}
```

## 响应结构

```json
{
  "data": {}
}
```

## 错误码

| 错误码 | 触发条件 | 用户提示 | 是否可重试 |
|---|---|---|---|
| `<code>` | `<条件>` | `<提示>` | `<是/否>` |

## 兼容性要求

- `<兼容性规则>`

