# 边界场景：<模块名>

状态：current
最后校验日期：<YYYY-MM-DD>

## 高风险场景

| 场景 | 风险 | 预期处理 | 验证方式 |
|---|---|---|---|
| `<场景>` | `<风险>` | `<处理>` | `<验证>` |

## 历史坑

| 问题 | 根因 | 防复发规则 |
|---|---|---|
| `<问题>` | `<根因>` | `<规则>` |

## 人工确认点

| 场景 | 需要用户确认什么 | 未确认时行为 |
|---|---|---|
| `<场景>` | `<确认项>` | `<行为>` |

