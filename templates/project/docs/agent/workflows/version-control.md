# Workflow：版本控制

## 目标

版本控制操作必须可审计、范围清晰，并避免提交无关改动。

## 步骤

1. 阅读入口、质量规则和本 workflow。
2. 检查工作区状态。
3. 区分本次改动和用户已有改动。
4. 运行必要验证。
5. 只暂存本次相关文件。
6. 使用清晰提交信息。
7. 推送前获得用户明确授权。

## 禁止事项

- 不允许提交未确认的无关改动。
- 不允许在验证失败后假装通过。
- 不允许未授权推送到远端。
- 不允许使用破坏性命令清理用户改动。

