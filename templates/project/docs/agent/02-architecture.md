# 架构说明

## 总体架构

```text
<在这里描述系统分层，例如 UI / API / Service / Storage / Worker>
```

## 架构原则

- `<原则 1>`
- `<原则 2>`
- `<原则 3>`

## 模块边界

| 模块 | 职责 | 不负责 |
|---|---|---|
| `<模块名>` | `<职责>` | `<不负责>` |

## 依赖方向

```text
<上层模块> -> <公开接口> -> <下层模块>
```

## 禁止事项

- `<禁止事项 1>`
- `<禁止事项 2>`

## 需要补充的架构问题

- `<待确认问题 1>`
- `<待确认问题 2>`

