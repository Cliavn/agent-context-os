# 渐进式接入状态

本文件记录 `<项目名>` 接入 Agent Context Engine 的状态。首次接入老项目时，不要求立即补齐全部文档。

## 接入概览

| 项 | 内容 |
|---|---|
| 接入状态 | `progressive` |
| 接入日期 | `<YYYY-MM-DD>` |
| 接入方式 | `<新项目完整接入 / 老项目渐进接入>` |
| 已有入口文件 | `<无 / AGENTS.md / Agent.md / 其他>` |
| 是否存在旧文档 | `<是 / 否 / 未检查>` |
| 是否建立旧文档索引 | `<是 / 否>` |
| 当前上下文可信度 | `<low / medium / high>` |

## 首次接入已完成

- [ ] 已创建或合并 `AGENTS.md`。
- [ ] 已创建 `docs/agent/00-index.md`。
- [ ] 已创建 `docs/agent/style-profile.md`。
- [ ] 已创建 `docs/agent/memory.md`。
- [ ] 已创建 `docs/agent/legacy-docs.md`。
- [ ] 已创建最小 workflow 和 checklist。
- [ ] 已说明首次接入不会全量分析旧文档。
- [ ] 已运行结构检查。

## 当前不做

- 不全量阅读旧文档。
- 不生成完整架构说明。
- 不迁移所有业务规则。
- 不把旧文档默认视为当前事实。
- 不要求用户先补齐全部模板。

## 后续按需补齐

| 触发场景 | 补齐内容 |
|---|---|
| 修改某个模块 | 补齐该模块文档 |
| 涉及业务规则 | 补齐相关业务语义 |
| 读取旧文档 | 更新 `legacy-docs.md` 状态 |
| 用户补充背景 | 写回 `memory.md` 或对应业务文档 |
| 用户补充风格偏好 | 写回 `style-profile.md` |
| 修复复发问题 | 补充历史问题和防复发规则 |

## 接入风险

- `<当前已知风险或缺口>`

## 下一步建议

- `<最小后续动作>`
