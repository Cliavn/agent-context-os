# 项目画像

## 项目目标

`<项目名>` 用于 `<一句话说明项目解决的问题>`。

## 非目标

本项目当前不做：

- `<非目标 1>`
- `<非目标 2>`
- `<非目标 3>`

## 核心用户

| 用户类型 | 主要诉求 |
|---|---|
| `<用户类型>` | `<诉求>` |

## 核心业务对象

| 对象 | 说明 |
|---|---|
| `<业务对象>` | `<说明>` |

## 当前阶段

状态：`planning / prototype / active-development / stabilization / maintenance`

说明：

- `<当前阶段说明>`

## 当前风险

- `<风险 1>`
- `<风险 2>`

