# 方案落实台账：<方案名称>

状态：draft
方案 ID：<plan-id>
来源：<对话摘要 / 用户说明 / 文档路径>
关联摄取记录：<docs/agent/intake.md#plan-id / 无>
创建时间：<YYYY-MM-DD>
最后确认人：<用户 / 负责人 / 待确认>
最后确认时间：<YYYY-MM-DD / 待确认>
替代关系：<无 / supersedes: plan-id / superseded-by: plan-id>

## 1. 方案目标

- `<本方案要解决的问题>`

## 2. 非目标

- `<本轮明确不做什么>`

## 3. 已确认点

| ID | 确认点 | 来源摘要 | 适用范围 |
|---|---|---|---|
| C1 | `<确认点>` | `<不是原始会话，只写结论摘要>` | `<模块 / 业务对象 / 页面 / 接口>` |

## 4. 待确认点

| ID | 问题 | 影响 | 需要谁确认 | 状态 |
|---|---|---|---|---|
| O1 | `<待确认问题>` | `<影响范围>` | `<用户 / 产品 / 技术负责人>` | `open` |

## 5. 开发顺序

| 顺序 | 任务 ID | 任务名称 | 依赖 | 状态 |
|---|---|---|---|---|
| 1 | T1 | `<任务名称>` | `<无 / T0>` | `pending` |

## 6. 任务清单

| 任务 ID | 任务 | 来源摘要 | 依赖 | 验收口径 | 状态 |
|---|---|---|---|---|---|
| T1 | `<要实现什么>` | `<讨论后确认的结论摘要>` | `<无 / T0>` | `<如何判断完成>` | `pending` |

## 7. 多 Agent 分工

| 任务 ID | Agent 角色 | 输入 | 输出 | 状态 |
|---|---|---|---|---|
| T1 | `<主 Agent / 检索 Agent / 审查 Agent / 验证 Agent>` | `<任务包>` | `<预期输出>` | `pending` |

## 8. 执行记录

| 时间 | 任务 ID | 状态变化 | 说明 | 验证 |
|---|---|---|---|---|
| `<YYYY-MM-DD>` | T1 | `pending -> done` | `<完成说明>` | `<命令或手工验收>` |

## 9. 文档同步计划

| 内容 | 写回位置 | 触发条件 | 状态 |
|---|---|---|---|
| `<稳定事实或规则>` | `<memory.md / 业务文档 / 模块文档 / 决策记录>` | `<任务完成并验证>` | `pending` |

## 10. 完成核对

- [ ] 所有 `confirmed` 任务均已完成，或已标记为 `blocked` / `deferred` 并说明原因。
- [ ] 所有验收口径均已验证，或已说明无法验证原因。
- [ ] 不存在无说明的 `pending` 任务。
- [ ] 已评估记忆、业务文档、模块文档、决策记录和项目风格画像是否需要写回。
- [ ] 最终报告已列出任务总数、完成、阻塞、延期和未开始数量。

## 11. 变更记录

| 时间 | 变更 | 原因 | 操作人 |
|---|---|---|---|
| `<YYYY-MM-DD>` | `<变更内容>` | `<原因>` | `<用户 / Agent>` |
