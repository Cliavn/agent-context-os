# 开发实现说明：<任务名称>

## 1. 修改目标

<本次要解决什么问题。>

## 2. 非目标

- `<本次明确不做什么>`

## 3. 变更等级

- 等级：`<S0 / S1 / S2 / S3>`
- 原因：`<为什么是这个等级>`
- 是否触发升级：`<否 / 升级原因>`

## 4. 业务规则引用

- `<业务文档路径>`

## 5. 项目风格画像引用

- 是否涉及项目风格画像：`是 / 否`
- 关联文档：`<无 / docs/agent/style-profile.md / 模块文档>`
- 影响说明：`<视觉 / 交互 / 文案 / 命名 / 分层 / 端侧习惯 / 无>`

## 6. 当前实现理解

<基于代码和文档确认的当前实现。>

## 7. 核心方案

<说明最小实现路径。>

## 8. 影响范围

| 模块 | 影响 | 风险 |
|---|---|---|
| `<模块>` | `<影响>` | `<风险>` |

## 9. 数据流与状态流

```text
<输入> -> <处理> -> <状态变化> -> <输出>
```

## 10. 历史问题检查

- 是否命中历史问题：`是 / 否`
- 关联问题：`<路径>`
- 防复发措施：`<测试 / checklist / 文档更新>`

## 11. 记忆回写计划

- 是否产生新项目事实或业务事实：`是 / 否`
- 是否产生新风格事实：`是 / 否`
- 写回位置：`<docs/agent/memory.md / docs/agent/style-profile.md / 业务文档 / 模块文档 / 问题库 / 无>`
- 开放问题：`<无 / 需要用户确认的问题>`

## 12. 方案摄取引用

- 关联方案：`<无 / plan-id>`
- 方案状态：`<无 / current / proposed / assumption / open / conflict>`
- 方案意图：`<摘要>`
- 系统影响矩阵：`<摘要或路径>`

## 13. 方案落实台账引用

- 台账路径：`<无 / docs/agent/plans/plan-id.md>`
- 台账状态：`<无 / draft / confirmed / active / done / superseded>`
- 本轮任务 ID：`<无 / T1, T2>`
- 开发顺序：`<摘要>`
- 验收口径：`<摘要>`

## 14. 验证矩阵

| 验证项 | 命令或方式 | 预期 |
|---|---|---|
| `<验证项>` | `<命令>` | `<预期>` |

## 15. 风险与回滚

- 风险：`<风险>`
- 回滚点：`<如何回滚>`
